version: '3.8'

services:
  vegeta-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vegeta-backend
    ports:
      - "8081:8081"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATA_MONGODB_URI=mongodb+srv://ngthtrong204:ngthtrong204@vegeta.3rwwtvh.mongodb.net/?retryWrites=true&w=majority&appName=Vegeta
      - SPRING_DATA_MONGODB_DATABASE=Vegeta
      - JWT_SECRET=Zqb+Z4udJMRRC1+Y+pxHRyawLoxs1IgJMV/9PoHDrTpf65dfcRK8pmFHRNZUYhol
      - JWT_EXPIRATION=86400000
      # OAuth2 Configuration
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
      - FACEBOOK_CLIENT_ID=${FACEBOOK_CLIENT_ID:-}
      - FACEBOOK_CLIENT_SECRET=${FACEBOOK_CLIENT_SECRET:-}
      - OAUTH2_REDIRECT_URI=${OAUTH2_REDIRECT_URI:-http://localhost:8081/api/v1/auth/oauth2/callback}
      # Frontend Configuration
      - FRONTEND_BASE_URL=https://project-front-end-nine.vercel.app/
      - FRONTEND_CALLBACK_PATH=/
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - vegeta-network

networks:
  vegeta-network:
    driver: bridge
